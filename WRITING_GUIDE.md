# 人生史の文章生成：編集設定と調整手順

## 方針

「プロのように」「感動的に」と指示するだけではなく、資料の選び方と段落の組み立て方を指定する。事実の厳密さと文章の豊かさを別々に管理する。

厚みは、架空の情景ではなく、本人が書いた行動・当時の考え・現在の振り返りを丁寧に扱うことで作る。学校名だけの時期と、具体的な回想のある転機を同じ長さで書かない。

## 調整できる設定

| 項目 | 標準 | 編集上の役割 |
| --- | --- | --- |
| 文字数の目安 | 2,000字 | 800〜5,000字。資料不足時の水増しは禁止。到達を保証しない |
| 語り手 | 三人称 | 一人称「私」に変更可能。本人の未記入の内面は代弁しない |
| 文体 | 温かく落ち着いた読み物 | 簡潔で率直な記録文にも変更可能 |
| 書き出し | 印象的な出来事・言葉 | 年代順にも変更可能。未記入の景色や会話を創作しない |
| 重点 | 資料の具体性と全体のバランス | 家族／仕事を厚くできるが、重点外の重要事実も落とさない |
| 性格・考え方 | 本人の説明を短く織り込む | 独立した短い章／人柄解説なしにも変更可能 |
| 本人の引用 | 印象的な言葉を少数 | 引用なしにも変更可能。思考の記述を発話に変えない |
| temperature | 0.35 | 0〜0.7。表現のばらつきであり、品質や創作の許可を表す値ではない |
| 追加の編集指示 | 空欄 | 構成・配分などを4,000文字以内で指定。事実保護ルールを解除できない |

temperatureはランダム性を調整する設定。高くすれば文章が良くなるわけではない。[Cloudflareのモデル仕様](https://developers.cloudflare.com/workers-ai/models/gpt-oss-120b/)

## 管理画面で試す

1. `/admin/story-preview`（管理者の「AI原稿確認」）を開く。
2. 利用者のJSONを選択。生成前の「確認すること」を読み、日付と年齢が食い違う場合は本人に確認する。
3. 文章づくりの設定を選び、必要なら追加の編集指示を入力する。
4. 「実際にAIへ渡す執筆指示を見る」で指示を確認する。
5. AI生成ボタンを押すと資料がCloudflare Workers AIへ送信される。生成による利用量が発生する。
6. 原稿を読み、比較する前に「確認結果をJSONで保存」を使う。次の生成は画面上の原稿を置き換える。

「この設定を保存」はこのブラウザの試作用設定だけを保存する。ファイル内の人生史・既存原稿の設定を読み込んでも、試作用設定を自動で変更しない。

「設定JSONを書き出す／読み込む」で開発者や別端末へ設定を渡せる。このファイルには人物の入力データは含めないが、追加指示に人物情報を書いた場合はその文章が含まれるので注意する。

生成された原稿の `writingSettings` と `editorialWarnings` は統合JSON内の `story` に保存される。管理画面の確認結果には、その画面で生成したときの追加指示も `editorialReview` に記録する。外部から読み込んだ既存原稿の追加指示は復元しない。

## 全利用者に適用する開発者の設定場所

- `lib/ai/writing-settings.ts`：標準値、許容範囲、出力上限（現在9,000トークン）。標準値の変更と再公開で利用者用の生成に反映される。
- `lib/ai/story-prompt.ts`：共通の事実保護ルール、段落構成、執筆・推敲手順。変更時は `STORY_PROMPT_VERSION` も更新する。
- `lib/ai/source-review.ts`：資料の日付チェック、追加取材ヒント、出力の簡易点検。
- `app/api/story/generate/route.ts`：生成呼び出しとメタデータ。モデルは既存の `@cf/openai/gpt-oss-120b` を維持。

管理画面の保存は全利用者への公開操作ではない。サーバーの共通設定として一括配信する仕組みは今回追加していない。

## 共通指示の要点

- 年齢メモと深掘りエピソードの両方から重要事実を拾う。結婚、子の誕生、継承などを照合し、書き落とさない。
- 詳しく書かれた転機を中心に、行動→当時の考え→現在の振り返りを自然につなぐ。未記入部分は飛ばす。
- 「学校生活が基盤になった」「将来も道を歩む」等の根拠のない総括で締めない。
- 性格回答の数値やJSONのキー名を本文に出さない。本人の自由記述を優先し、現在の傾向を過去の行動の原因にしない。
- 書いた後で、事実の欠落、時間の整合、架空の感情、重複、抽象語、箇条書きを点検する。作業過程は本文に出さない。

この推敲指示は1回の生成内の手順であり、独立した2回目のAI校閲ではない。生成費用を抑えるため、自動再生成も行わない。

## 資料チェックの限界

日本語の明示的な西暦（例：2015年9月）と生年月日・入力年齢を機械的に比較する。本文中の日付が別の出来事を指す場合もあるため、「誤り」と決めつけず確認事項として扱う。元JSONは変更しない。年齢と年月が食い違う箇所では年齢の断定を避ける指示を加える。

和暦・すべての自然言語表現・人間関係・出生順まで検証するものではない。月だけの記述から年を決めない。文章の簡易点検は箇条書き、資料ラベルの漏れ、出力上限の検知に限られ、事実性や重要事実の網羅を保証しない。

## 比較・評価

同じ資料で設定を一項目ずつ変え、結果JSONを別ファイルで保存する。違う設定の既存原稿を「新しい設定で生成した」と扱わない。

評価は、事実性（最優先）、重要事実の網羅、具体性、本人の言葉の保持、段落のつながり、過剰な美化がないこと、の順で行う。誤りがあれば文章の美しさより先に直す。

材料が足りなければ、AIの表現を盛るのではなく追加取材をする。例えば「総合力がついた」の次に、実際に写真や文章が役に立った場面を一つ聞く。「お寺を継いだ」の次には、引き継いだ具体的な仕事や当時気になっていたことを聞く。成長や葛藤があったことを前提にしない。

添付された個人JSONはリポジトリに保存せず、AIへの再送信も自動では行わない。テストには架空の人物のデータを使う。
