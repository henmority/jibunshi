# Public origin used to build absolute Open Graph and X image URLs.
# Set this to the final custom domain in Cloudflare Workers Builds.
SITE_URL=https://admin.example.com

# Production authentication is stored with `wrangler secret put`; never add
# ADMIN_PASSWORD or AUTH_COOKIE_SECRET to this file or Git.
